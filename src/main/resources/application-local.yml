spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/register_db
    username: myuser
    password: secret
  jpa:
    show-sql : true
    properties:
      hibernate:
        format_sql: true